'use strict'

/* eslint-env mocha */
const cypressParcelPreprocessor = require('.')

describe('cypress-parcel-preprocessor', () => {
  it('write this test', () => {
    console.assert(cypressParcelPreprocessor, 'should export something')
  })
})
